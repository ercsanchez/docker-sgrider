version: '3'
services:
  postgres:
    image: 'postgres:15-alpine'
  redis:
    image: 'redis:7-alpine'
  server:
    build:
      context: ./server
      dockerfile: Dockerfile.dev
    volumes:
      - /app/node_modules
      - ./server:/app